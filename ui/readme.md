This subproject contains:
1. User friendly composables with sensible defaults in `ui.kt`
2. The main entrypoint for creating maple composables(`runMaple()`)
3. The buildscript for bundling all the subprojects as well as the kotlin stdlib into a mod.