This subproject contains the source code of the schema, as well as the `api`, `compose`, `modifier` and  `widget` subprojects.

The former contains types that are shared for both internal use in this project as well as for you(The developer using this library) to use.
The latter 3 will hold the code generated by redwood. This can be done in a step by running `:schema:codeGen`.